---
title: "Untitled"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


```{r}
# library(fullcalendR)
pkgload::load_all()
library(shiny)

data = data.frame(title = paste("Event", 1:3),
                  start = c("2020-05-01", "2020-05-01", "2020-05-15"),
                  end = c("2020-05-02", "2020-05-04", "2020-05-18"),
                  color = c("red", "blue", "green"))

# taglist(
fullcalendar(
  data = data,
  # settings = list(selectable = TRUE,
  #                 dayClick = htmlwidgets::JS(
  #                   'function(date) {alert("click" + date.format());}')
  #                 ),
  elementId = "calendar")
# )
```

